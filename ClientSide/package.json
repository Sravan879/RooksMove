{
  "name": "rook-move",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "express": "^4.18.2",
    "phaser": "^3.60.0",
    "socket.io": "^4.7.2",
    "socket.io-client": "^4.7.2"
  }
}
{
  "name": "my-package",
  "scripts": {
    "build": "babel src -d lib",
    "test": "jest"
  }
}
